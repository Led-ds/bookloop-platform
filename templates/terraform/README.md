# Template — Módulo Terraform

Esqueleto de um módulo coeso seguindo `docs/standards/terraform.md`. Copie para
`terraform/modules/<nome>` e preencha.

Arquivos: `main.tf`, `variables.tf`, `outputs.tf`, `versions.tf`, `README.md`.
