# Prompt — Implementar endpoint (backend)

Contexto: siga `docs/standards/backend.md`. Clean Architecture, Rich Domain Model, DTO record,
MapStruct, Jakarta Validation, envelope `ApiResponse<T>`, erros no `@RestControllerAdvice`,
Flyway para schema. Não altere a arquitetura.

Tarefa:
> Implemente `<MÉTODO> <ROTA>` que `<comportamento>`.
> Entradas: `<payload/params>`. Regras de negócio: `<regras>`.
> Domínio afetado: `<entidades/serviços>`.
> Entregue: código por camada (domain/application/api), migração Flyway se necessário,
> e um teste de integração (Testcontainers). Liste suposições.
