# Prompt — Criar tela (frontend)

Contexto: siga `docs/standards/frontend.md`. React 19 + TS, TanStack Query, Zustand quando
necessário, tipos batendo com o backend (enums idênticos), estados de loading/erro.

Tarefa:
> Crie a tela `<nome>` que consome `<MÉTODO> <ROTA>` e permite `<ações>`.
> Contrato: `<shape da resposta>`. Enums do backend: `<lista>`.
> Entregue componente(s) + hook de dados + tratamento de erro. Liste suposições.
