# Prompt — Política IAM de privilégio mínimo

Contexto: siga `docs/standards/aws.md` e `security.md`. Nada de `*` amplo.

Tarefa:
> Para o componente `<serviço>` que precisa `<ações>` sobre `<recursos>`, escreva a policy IAM
> mínima (JSON) e a role associada em Terraform. Justifique cada permissão. Aponte riscos residuais.
